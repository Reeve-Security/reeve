- name: Apply finance demo persona
  ansible.builtin.include_role:
    name: mcp_catalog
  vars:
    demo_assistants:
      - codex-cli
      - cursor

- name: Plant finance approvals
  ansible.builtin.include_role:
    name: approval_state

- name: Plant finance secret fixtures
  ansible.builtin.include_role:
    name: planted_secrets

- name: Plant finance vulnerable-version placeholders
  ansible.builtin.include_role:
    name: vulnerable_version_evidence
