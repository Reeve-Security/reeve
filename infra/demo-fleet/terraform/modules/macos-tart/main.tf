# Tart orchestration runs on the existing Mac mini and is intentionally
# documented as an input contract here. The first recording pass should use
# reviewed shell/Ansible steps on that host before adding a custom provider.

