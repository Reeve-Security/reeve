name: ci

# Doc-only pushes (README + legal docs + docs/) skip CI to avoid paying for a
# full Blacksmith build/test matrix on a prose change. Contract-checked docs
# (schema/error-codes.md, SPEC.md, CONTRIBUTING.md, policies/README.md, tools/**)
# and the .gitignore boundary are intentionally NOT ignored, so a change that
# can break a CI contract still runs.
on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - 'CODE_OF_CONDUCT.md'
      - 'CLA.md'
      - 'SECURITY.md'
      - 'docs/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - 'CODE_OF_CONDUCT.md'
      - 'CLA.md'
      - 'SECURITY.md'
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft != true
    name: gitleaks
    # GitHub-hosted (free for this public repo): a ~10s secret scan does not need
    # Blacksmith hardware. Version stays pinned via .gitleaks-version (#35, #38).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.0

      - name: Install gitleaks
        run: |
          set -euo pipefail
          # Pinned in one repo-owned place (.gitleaks-version) so CI cannot
          # silently drift; scripts/check-gitleaks-pinning.py enforces it (#35).
          VERSION="$(tr -d '[:space:]' < .gitleaks-version)"
          curl -fsSL -o gitleaks.tar.gz "https://github.com/gitleaks/gitleaks/releases/download/v${VERSION}/gitleaks_${VERSION}_linux_x64.tar.gz"
          tar -xzf gitleaks.tar.gz gitleaks
          chmod +x gitleaks
          sudo mv gitleaks /usr/local/bin/gitleaks
          gitleaks version

      - name: gitleaks detect
        run: |
          set -euo pipefail
          mkdir -p gitleaks-reports
          report="gitleaks-reports/detect.json"
          set +e
          gitleaks detect \
            --source . \
            --verbose \
            --redact \
            --config .gitleaks.toml \
            --report-format json \
            --report-path "${report}"
          status="$?"
          set -e
          {
            echo "## gitleaks detect"
            echo
            echo "- config: \`.gitleaks.toml\`"
            echo "- report: \`${report}\`"
            echo "- redacted: yes"
            echo "- status: \`${status}\`"
            echo
          } >> "${GITHUB_STEP_SUMMARY}"
          if [ "${status}" -ne 0 ]; then
            echo "::error::gitleaks found potential leaks"
            python3 - "${report}" "${GITHUB_STEP_SUMMARY}" <<'PY'
          import json
          import pathlib
          import sys

          report = pathlib.Path(sys.argv[1])
          summary = pathlib.Path(sys.argv[2])
          try:
              findings = json.loads(report.read_text())
          except Exception as exc:
              raise SystemExit(f"failed to read gitleaks JSON report: {exc}")

          lines = [
              "### Findings",
              "",
              "| Rule | File | Line | Commit | Description |",
              "|---|---|---:|---|---|",
          ]
          for finding in findings:
              rule = finding.get("RuleID", "<unknown>")
              path = finding.get("File", "<unknown>")
              line = finding.get("StartLine", "<unknown>")
              commit = finding.get("Commit", "")
              description = finding.get("Description", "")
              secret = finding.get("Secret", "")
              match = finding.get("Match", "")
              print(
                  f"gitleaks finding: rule={rule} file={path} line={line} "
                  f"commit={commit} description={description} "
                  f"secret_redacted={bool(secret)} match_redacted={bool(match)}"
              )
              lines.append(
                  f"| `{rule}` | `{path}` | {line} | `{commit}` | {description} |"
              )
          summary.open("a").write("\n".join(lines) + "\n")
          PY
            exit "${status}"
          fi

      - name: Upload gitleaks report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: gitleaks-report
          path: gitleaks-reports/
          if-no-files-found: ignore
          retention-days: 7

      - name: gitleaks positive control
        run: |
          set -euo pipefail
          tmp_dir="$(mktemp -d)"
          trap 'rm -rf "${tmp_dir}"' EXIT
          # Assemble the fake token at runtime so the literal never sits in
          # this file (otherwise the main `gitleaks detect` scan flags it).
          token_prefix='ghp_'
          printf 'GITHUB_TOKEN=%s%s\n' "${token_prefix}" '1234567890abcdefghijklmnopqrstuvwxyzABCD' > "${tmp_dir}/leak.env"
          set +e
          gitleaks dir "${tmp_dir}" \
            --redact \
            --config .gitleaks.toml \
            --report-format json \
            --report-path "${tmp_dir}/report.json"
          status="$?"
          set -e
          if [ "${status}" -eq 0 ]; then
            echo "expected gitleaks to reject fake GitHub token" >&2
            exit 1
          fi
          if ! grep -Eq '"RuleID"[[:space:]]*:[[:space:]]*"github-token-extended"' "${tmp_dir}/report.json"; then
            echo "expected fake GitHub token to match github-token-extended rule" >&2
            cat "${tmp_dir}/report.json" >&2
            exit 1
          fi

  check:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft != true
    name: check (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON('{"include":[{"os":"ubuntu","runner":"blacksmith-4vcpu-ubuntu-2404"},{"os":"macos","runner":"blacksmith-6vcpu-macos-latest"}]}') }}
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.0

      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (pinned 2026-03-27)
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2.8.1
        with:
          shared-key: ci-${{ matrix.os }}

      - name: Install OPA
        run: |
          set -euo pipefail
          OPA_VERSION="1.15.2"
          case "${RUNNER_OS}-$(uname -m)" in
            Linux-x86_64)
              OPA_ASSET="opa_linux_amd64_static"
              OPA_SHA256="a9d9481e463e7af8cb1a2cd7c3deb764f0327b3281c54e632546c2f425fc0824"
              ;;
            Linux-aarch64|Linux-arm64)
              OPA_ASSET="opa_linux_arm64_static"
              OPA_SHA256="6651bf5a80cfec6ba6a2d3b6a550b8f748d9cade1c74d54b5f854782f9bea67a"
              ;;
            macOS-arm64)
              OPA_ASSET="opa_darwin_arm64"
              OPA_SHA256="0622a4ecce925594a240484e773eee2773c55627cc9bdaae885e8bb2a74673e2"
              ;;
            macOS-x86_64)
              OPA_ASSET="opa_darwin_amd64"
              OPA_SHA256="34db678edd97adf6f0b4ed7cb20c2d1e81e7ab793e5ca6f08e734c0473c47b8e"
              ;;
            *)
              echo "unsupported runner/architecture: ${RUNNER_OS}-$(uname -m)" >&2
              exit 1
              ;;
          esac
          curl -fsSL -o opa "https://openpolicyagent.org/downloads/v${OPA_VERSION}/${OPA_ASSET}"
          echo "${OPA_SHA256}  opa" | shasum -a 256 --check
          chmod +x opa
          sudo mv opa /usr/local/bin/opa
          opa version

      - name: Install cosign
        uses: sigstore/cosign-installer@d7d6bc7722e3daa8354c50bcb52f4837da5e9b6a # v3.8.1

      - name: Install Linux trace tooling
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y strace
          strace -V

      # The local check list is owned by scripts/merge-gate.sh (single source of
      # truth) so it cannot drift between CI and local runs. gitleaks and
      # windows-release-smoke remain separate jobs below.
      - name: merge gate (local checks)
        shell: bash
        run: bash scripts/merge-gate.sh --ci-local

  windows-release-smoke:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft != true
    name: windows release smoke
    runs-on: blacksmith-4vcpu-windows-2025
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.0

      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (pinned 2026-03-27)

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2.8.1
        with:
          shared-key: windows-release-smoke

      - name: Install cargo-dist
        shell: pwsh
        run: irm https://github.com/axodotdev/cargo-dist/releases/download/v0.31.0/cargo-dist-installer.ps1 | iex

      - name: Install cosign
        uses: sigstore/cosign-installer@d7d6bc7722e3daa8354c50bcb52f4837da5e9b6a # v3.8.1

      - name: Verify toolchain
        shell: pwsh
        run: |
          rustc --version
          cargo --version
          dist --version
          cosign version

      - name: Run Windows scanner tests
        shell: pwsh
        run: cargo test -p aibom-scanner windows_

      - name: Build Windows release artifact
        shell: pwsh
        run: dist build --allow-dirty --print=linkage --output-format=json --artifacts=local --target=x86_64-pc-windows-msvc
